import React from 'react';
import PropTypes from 'prop-types';

const Offers = () => {
    return (
        <div>
            orffer
        </div>
    );
};

Offers.propTypes = {};

export default Offers;